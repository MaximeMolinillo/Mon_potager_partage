{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}

	<main class="container">
		<section class="row">
			<div class="col-12">
				<h1>Mon Potager Partagé</h1>
				<h5>La permaculture participative</h5>
			</div>
			{% for encyclopedie in encyclopedie %}
				<div class="col-4">
					<div class="card h-100" style="width: 18rem;">
						{# <img src="..." class="card-img-top" alt="..."> #}
						<div class="card-body">
							<h5 class="card-title">{{ encyclopedie.titre }}</h5>
							<p class="card-text">{{ encyclopedie.description }}</p>
							<a href="#" class="btn btn-primary">Voir la fiche</a>
						</div>
					</div>
				</div>
			{% endfor %}


			<div class="col-12">
				{{ dump() }}
			</div>
		</section>
	</main>

{% endblock %}
